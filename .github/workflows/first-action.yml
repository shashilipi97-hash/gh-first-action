name: first-workflow
on: workflow_dispatch
jobs:
  name: first-job
  runs-on: Ubuntu-latest
  steps:
    - name: Print greeting
      run: echo "Hello world"
    - name: Print goodbye
      run: echo "Done! -bye"
